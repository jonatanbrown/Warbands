<% if @user.team %>
  <% if current_user.battle %>
    <p><%= battle_button("Already in battle") %></p>
  <% else %>
    <p><%= link_to "Queue for match", queue_path(@user) %></p>
  <% end %>
  <p><%= link_to "Smithy", smithy_path %></p>
<% else %>
 <p>You have no teams, create one here!</p>
 <%= link_to "Create Team", new_team_path %>
<% end %>

